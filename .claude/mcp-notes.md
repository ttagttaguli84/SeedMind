# MCP Notes — Staging Area

작업 중 발견된 MCP 제한·우회 패턴을 즉시 기록하는 임시 문서.
세션 종료 전 `CLAUDE.md` → `MCP 알려진 제한사항` 테이블로 병합 후 이 파일 내용 초기화.

## 미병합 항목

_(현재 없음 — farming-tasks 발견 사항은 CLAUDE.md에 병합 완료)_

---

## 병합 기준

| 조건 | 처리 |
|------|------|
| 작업 중 새 제한/우회 발견 | 즉시 이 파일 `미병합 항목`에 추가 + 커밋 |
| 사용자가 명시적으로 요청할 때 | CLAUDE.md 테이블에 병합 → 이 파일 `미병합 항목` 초기화 → 커밋 |
| 동일 패턴 2회 이상 재발 | CLAUDE.md 항목에 `(반복 확인)` 태그 추가 |
